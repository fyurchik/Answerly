<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Answerly" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-sans bg-page-light">
    <%= render 'shared/navbar' %>

    <!-- Flash Messages -->
    <% if notice.present? || alert.present? %>
      <div class="container mx-auto px-4 mt-4">
        <% if notice %>
          <div class="bg-success-bg-light border-l-4 border-success p-4 rounded mb-4">
            <p class="text-success-dark font-medium"><%= notice %></p>
          </div>
        <% end %>
        <% if alert %>
          <div class="bg-error-bg-light border-l-4 border-error p-4 rounded mb-4">
            <p class="text-error-dark font-medium"><%= alert %></p>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="container mx-auto px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>
