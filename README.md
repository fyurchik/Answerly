# Answerly

AI-додаток для відповідей на питання, створений на Ruby on Rails, який надає інтелектуальні відповіді за допомогою OpenAI GPT.

## Можливості

- 🤖 AI-відповіді за допомогою OpenAI GPT
- 💬 Чат-інтерфейс у реальному часі з потоковими відповідями
- 📚 Історія питань та їх управління
- 🎨 Сучасний, адаптивний інтерфейс на Hotwire
- 🔄 Підтримка WebSocket для оновлень у реальному часі
- 🌐 Інтеграція з ngrok для публічного доступу

## Технології

- Ruby 3.3.6
- Rails 8.0.0
- PostgreSQL
- Hotwire (Turbo & Stimulus)
- OpenAI API
- ngrok для тунелювання

## Вимоги

- Ruby 3.3.6
- PostgreSQL
- Node.js та Yarn
- Обліковий запис ngrok (для публічного доступу)

## Налаштування

1. **Клонувати та встановити залежності:**
   ```bash
   git clone <repository-url>
   cd answerly
   bundle install
   yarn install
   ```

2. **Налаштувати змінні середовища:**
   ```bash
   cp .env.example .env
   ```

   Відредагуйте `.env` та додайте свої облікові дані:
   ```env
   # Налаштування OpenAI
   OPENAI_API_KEY=ваш_ключ_openai
   OPENAI_MODEL=gpt-4-turbo-preview

   # Налаштування ngrok (для публічного доступу)
   NGROK_AUTHTOKEN=ваш_токен_ngrok
   NGROK_DOMAIN=ваш-домен.ngrok-free.app  # Опціонально: якщо у вас є статичний домен

   # Налаштування бази даних
   DATABASE_URL=postgresql://localhost/answerly_development

   # Налаштування Rails
   RAILS_ENV=development
   SECRET_KEY_BASE=ваш_секретний_ключ
   ```

3. **Налаштувати базу даних:**
   ```bash
   rails db:create
   rails db:migrate
   rails db:seed  # Опціонально: завантажити тестові дані
   ```

4. **Запустити додаток:**
   ```bash
   bin/dev
   ```

   Додаток буде доступний за адресами:
   - Локально: `http://localhost:3000`
   - ngrok: Перевірте вивід у терміналі для публічної URL

## Отримання API ключів

### Ключ OpenAI API
1. Перейдіть на https://platform.openai.com/api-keys
2. Зареєструйтесь або увійдіть
3. Створіть новий API ключ
4. Додайте інформацію про оплату (потрібно для доступу до API)
5. Скопіюйте ключ у ваш `.env` файл

### Налаштування ngrok
1. Зареєструйтесь на https://ngrok.com
2. Отримайте ваш authtoken з https://dashboard.ngrok.com/get-started/your-authtoken
3. Додайте токен у ваш `.env` файл
4. (Опціонально) Налаштуйте статичний домен у панелі ngrok для постійних URL

## Змінні середовища

| Змінна | Обов'язкова | Опис |
|--------|-------------|------|
| `OPENAI_API_KEY` | Так | Ваш ключ API OpenAI для доступу до GPT |
| `OPENAI_MODEL` | Ні | Модель GPT для використання (за замовчуванням: gpt-4-turbo-preview) |
| `NGROK_AUTHTOKEN` | Так* | Токен автентифікації ngrok для публічного доступу |
| `NGROK_DOMAIN` | Ні | Власний домен ngrok (для платних планів) |
| `DATABASE_URL` | Ні | Рядок підключення до PostgreSQL |
| `RAILS_ENV` | Ні | Середовище Rails (development/production) |
| `SECRET_KEY_BASE` | Так** | Секретний ключ Rails |

\* Обов'язково, якщо ви хочете публічний доступ через ngrok  
\** Обов'язково для продакшн

## Як запустити

### Режим розробки
```bash
bin/dev
```
Це запускає:
- Сервер Rails на порту 3000
- Процес збірки CSS/JS
- Тунель ngrok (якщо налаштовано)

### Альтернатива: Запустити компоненти окремо
```bash
# Термінал 1 - Сервер Rails
rails server

# Термінал 2 - Збірка CSS/JS
yarn build --watch

# Термінал 3 - ngrok (опціонально)
ngrok http 3000
```

### Доступ до додатку
- **Локально:** http://localhost:3000
- **Публічно (ngrok):** Перевірте термінал для URL ngrok

## Вирішення проблем

### Помилка ліміту запитів (429)
- Перевірте використання API OpenAI на https://platform.openai.com/account/usage
- Переконайтесь, що у вас є доступні кредити
- Додайте спосіб оплати, якщо ви на безкоштовному тарифі
- Зачекайте кілька хвилин перед повторною спробою

### Проблеми з підключенням ngrok
- Перевірте правильність вашого authtoken
- Перевірте статус ngrok: https://status.ngrok.com
- Переконайтесь, що жоден firewall не блокує ngrok
- Спробуйте `ngrok http 3000` вручну для тесту

### Помилка підключення до бази даних
- Переконайтесь, що PostgreSQL запущено: `brew services start postgresql`
- Перевірте облікові дані бази даних у `.env`
- Переконайтесь, що база даних існує: `rails db:create`

## Ліцензія

MIT